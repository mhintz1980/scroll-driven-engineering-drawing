# Execution Checkpoint

**Current Task:** Interactive Project Galleries
**Exact Stop Point:** Branch is `main`. The live Vite portfolio app now has the requested project-card hover-lift treatment and manual left/right arrow controls in the hero media block. Current uncommitted work is in `src/components/sections/ProjectCard.tsx`, `src/components/sections/projects-media.ts`, and `src/components/sections/projects-media.test.ts`. Existing user-owned edits are still present in `ui-text-cheat-sheet.md`.

### What is done:
- [x] Investigate how the `Parallax` component is implemented on the background.
- [x] Stretch the parallax rendering down to include the `references` section.
- [x] Pin the bottom CTA section to remain a solid background and change the text to `Let's create something awesome.`
- [x] Inline the hero cycler component and adjust baseline using a precise translate offset.
- [x] Create a markdown "cheat sheet" artifact covering editable site text and color/shade locations.
- [x] Refactor the Project card image blocks into a cleaner React boundary.
- [x] Add a slow auto-play cycle feature to the main image of each project card.
- [x] Add fade transitions and staggered autoplay timing so project cards do not all switch frames together.
- [x] Add the armament project card and move PumpTracker to the last slot with Pump Package second to last.
- [x] Add a lifted Framer Motion hover treatment so the project hero image can rise above neighboring cards instead of staying clipped inside the card shell.
- [x] Add manual left/right pagination arrows to the project hero image experience.
- [x] Add a wrap-back helper in `projects-media.ts` and cover it in `projects-media.test.ts`.
- [x] Verify the current gallery changes with targeted tests, production build, and a browser check on the local Vite app.

### What remains (Next Session):
- [ ] Review the hover-lift sizing, offsets, and arrow visibility on mobile/tablet breakpoints and tune if the interaction feels too aggressive.
- [ ] Decide whether the gallery arrows should pause autoplay for a short window after manual interaction.
- [ ] If desired, add component-level UI tests for `ProjectCard` behavior instead of relying only on helper tests plus manual browser verification.

### Decisions made:
- Keep the project cards manufacturing-first. Mechanical projects remain primary signal and software stays supporting proof.
- Keep the refactor scoped to the live portfolio React components, not Remotion scenes or old static scripts.
- Centralize project media normalization in `src/components/sections/projects-media.ts` and keep card rendering in `src/components/sections/ProjectCard.tsx`.
- Keep autoplay logic lightweight and local to `ProjectCard`, with frame wrapping handled by pure helpers in `projects-media.ts`.
- Let the outer card shell stop clipping the hero so the hovered media stage can visually rise over adjacent content, while keeping clipping on the inner media frame itself.
- Keep the arrow controls tied to the existing `selectedMediaIndex` state so autoplay, expanded media strip selection, and manual navigation all stay aligned.

### Commands already run:
- `npm test -- src/components/sections/projects-media.test.ts`
- `npm run build`

### Exact next action:
- Start by reviewing `src/components/sections/ProjectCard.tsx` in a browser at desktop and mobile widths to decide whether the hover lift scale/offset should be reduced.
- If the current interaction is acceptable, choose the next follow-up: autoplay pause-after-click, responsive polish, or component-level UI tests.

### Files worth touching first:
- `src/components/sections/ProjectCard.tsx`
- `src/components/sections/projects-media.ts`
- `src/components/sections/projects-media.test.ts`
- `src/components/sections/Projects.tsx`

### Blockers:
- None
