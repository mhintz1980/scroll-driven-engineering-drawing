---
phase: 3d-perspective-rig
task: complete
total_tasks: 6
status: complete
last_updated: 2026-05-08T22:10:00Z
---

<current_state>
The 3D perspective scroll-driven drawing rig is fully complete and pushed.
Two ProjectZone stations are planted on the substrate and verified via Playwright.
The next session should begin with content work or new feature additions.
</current_state>

<completed_work>
- Task 1: SVG asset integrated — `invert(1)` white linework on dark background.
- Task 2: CSS 3D stage — `perspective: 4000px` on container, `preserve-3d` on substrate.
- Task 3: GSAP `rotateX` tilt — flat transit, 35° tilt stop (fixed from original 62° geometry bug).
- Task 4: ProjectZone `z: 400` lift with `transformStyle: preserve-3d`.
- Task 5: Depth-of-field blur on background layer via `onLift` callback.
- Task 6: Camera recalibration — iterative Playwright screenshots, Station B planted.
- FOUC fix — `useLayoutEffect`, initial opacity-0/scale-0 state on all ProjectZone elements.
- Lint fix — `perspective: '500px'` on labelRef (was invalid `transformPerspective: 500`).
</completed_work>

<current_state_and_coordinates>
## CURRENT STATE & COORDINATES (THE MAP)
> This section MUST be updated by the AI whenever a new spatial component is added,
> a coordinate is adjusted, or any geometry constant changes. Update it mid-session if needed.

### Map Substrate (The 3D Rig)
- **Asset:** `AR-15-Lower-Reciever-v2.svg` (`invert(1)` filter for white linework, `mix-blend-screen` on dark background)
- **Native Resolution:** 8800px (W) × 6800px (H)
- **Geometry:** Outer container `perspective: 4000px`, substrate `transformStyle: preserve-3d`
- **Max safe substrate Y** at `scale: 1.2`: `4000 / (sin(35°) × 1.2) ≈ 5806px` — never exceed this or content goes past focal plane

### Active Stations & Camera Stops

**Station A** [`id="A"`, TRIGGER GUARD RADIUS]
- Component Position: `left: 1450px, top: 3200px`
- Camera Stop: `x: -1400, y: -3730, scale: 1.2, rotateX: 0` *(flat start — triggers DOF blur via `onLift`)*

**Station B** [`id="B"`, BUFFER TUBE SOCKET]
- Component Position: `left: 5567px, top: 833px`
- Camera Stop: `x: -6400, y: -1000, scale: 1.2, rotateX: 35` *(35-degree tilted stop — no `onLift`, DOF already active)*

**[Hero / Title Block]** — Pending placement.
</current_state_and_coordinates>

<decisions_made>
- `invert(1)` filter converts black-on-transparent SVG to white linework.
- `mix-blend-screen` makes white linework visible; transparent areas show dark `bg-slate-950`.
- DOF blur state must include `invert(1)` or linework turns black.
- `perspective` on parent, `preserve-3d` on element — CSS 3D hierarchy requirement.
- `useLayoutEffect` is mandatory for GSAP initial state (fires before paint).
</decisions_made>

<next_action>
Rig is complete. Choose a direction:
1. Add real video/image content inside the ProjectZone detail circles.
2. Add Station C at a third drawing detail (new whip-pan stop).
3. Integrate title block / general notes into the drawing-package layout.
4. Hook the Drawing Package page into main portfolio navigation.
</next_action>
