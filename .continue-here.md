---
last_updated: 2026-05-16T18:41:00Z
status: Projection pass verified and Phase 4 prune complete. GSAP coordinate bug fixed with CSS transitions for precise tracking.
---

# Session Context: Spatial Portfolio

> **Agent Context Protocol:** To save context window, read docs in this exact order:
> 1. **This file (`.continue-here.md`)**: Read fully. It is the live execution checkpoint and coordinate map.
> 2. **`PRODUCT.md`**: Read for register, users, and anti-references.
> 3. **`DESIGN.md`**: Read for the live visual system and blueprint constraints.
> 4. **`AGENTS.md`**: Read to understand the current rig architecture, coordinates, and protected decisions.
> 5. **Newest relevant handoff in `docs/plans/`**: Currently `docs/plans/2026-05-15-pixelation-navigation-handoff.md`, then `docs/plans/2026-05-15-phase3-calibration-handoff.md` if needed for calibration detail.
> 6. **`docs/the-handoff-and-plan-from-other-agent-session.md`**: Only read from line 329 onward.
> 7. **`docs/the-plan-worth-doing.md`**: Read only if you need the broader project vision.

---

## CURRENT TASK

Projection pass verification is next. Verify A/B/C/D and mobile, tune the screen-space projection origin/object placement, then resume Phase 4 prune.

## WHAT IS DONE

### Phase 1 — Compile recovery (this session)
- Null-guarded hero `querySelector` results in the intro GSAP timeline
- Created `STATION_A/B/C/D_LAYOUT` objects with `pathD`, `anchor`, `circleStyle`
- Added `active` prop + `useEffect` trigger to `TitleBlockStation` (mirrors ProjectZone pattern)
- Build: ✅ green

### Phase 2 — Substrate swap (this session)
- `SUBSTRATE_W = 1625`, `SUBSTRATE_H = 1075`
- `SUBSTRATE_ASSET = 'Lower Receiver_Final.svg'`
- `SUBSTRATE_NEEDS_INVERT = false` — no runtime `invert(1)` or `mix-blend-screen`
- All station `cx/cy` divided by X_SCALE=5.415 / Y_SCALE=6.326
- Station camera zoom: 1.2 → 6.5 (compensates for smaller substrate)
- Hero formula constants updated: `1600→295`, `560→89`, `0.52→2.82`
- Hero text position: `(2000,2000)→(369,304)`, width: `1600px→295px`
- Hero text font sizes scaled down: `180px→33px`, `32px→6px`, `28px→5px`, etc.
- ProjectZone positions recalculated for new coordinate space
- TitleBlockStation position: `(6400,5800)→(1182,917)`, width: `1000px→185px`
- Build: ✅ green, Runtime: ✅ zero console errors

### Phase 3 — Visual calibration (this session)
- Fixed ProjectZone first-mount animation: inactive circles/labels stay hidden on hero load
- Removed IntersectionObserver as the runtime trigger path for ProjectZone and TitleBlockStation; `active` is canonical
- Corrected hero camera from close detail transform to full drawing overview:
  - `cx: 812.5`, `cy: 537.5`, `scale: wide`, `rotateX: 0`
- Rebuilt hero nameplate sizing for the overview scale:
  - position `left: 812.5px`, `top: 537.5px`, width `920px`
- Scaled ProjectZone detail geometry for native substrate zoom:
  - detail circle size `clamp(34px, 5.7vw, 50px)`
  - detail lift `z: 75`
- Recalibrated ProjectZone positions and layout:
  - A: top `278px`, left `0px`, anchor `(291,250)`, circle `(left 285px, top 235px)`
  - B: top `0px`, left `748px`, anchor `(300,134)`, circle `(left 285px, top 150px)`
  - C: top `442px`, left `378px`, anchor `(300,250)`, circle `(left 285px, top 240px)`
  - D: top `411px`, left `988px`, anchor `(300,250)`, circle `(left 285px, top 245px)`
- Recalibrated TitleBlockStation:
  - target `cx: 1274`, `cy: 953`, `scale: 5.4`, `rotateX: 0`
  - component `top: 910px`, `left: 1196px`, `width: 130px`
- Build: ✅ green
- Browser verification: ✅ 975×550, 1280×720, 1440×900, 1920×1080
- Calibration mode: ✅ `?calibrate=1` forces A/B/C/D circles, labels, and title block visible

### Phase 3.5 — Pixelation and Scaling Fix (this session)
- Fixed Hero section CSS layout bug where Tailwind `max-width: 100%` was squishing the 6500px background image.
- Reduced `RENDER_SCALE` from 4 to 2 in `DrawingPackagePage.tsx` to fix GPU max texture size limits causing blackness on stations B, C, D, T.
- Added scale factor `S = 0.1846` to `ProjectZone.tsx` to proportionally shrink the massive UI layout to fit the new native `1625×1075` coordinate grid.

### Phase 3.6 — Navigation & Content Pass (this session)
- Replaced buggy scroll buffering logic with a deliberate `wheelAccumulator` and 900ms input lockout to entirely fix trackpad navigation overshoot.
- Added `videoSrc` to `ProjectZoneProps` and replaced the old static image references with `engineering-review-loop.mp4`.
- Renamed station titles to match actual portfolio data ("ARMAMENT COMPONENTS & RECEIVER SYSTEMS", etc.).
- Adjusted CSS math in `ProjectZone` to keep font text crisp during the 6.5x viewport zoom.

### Phase 3.7 — ProjectZone geometry fidelity (this session)
- Standardized active Detail View Circle screen size across stations:
  - Verified at `975×550`: A `214×214`, B `214×214`, C `215×213`, D `213×214`
  - Verified at `1440×900`: A `214×214`, B `214×214`, C `215×213`, D `213×214`
- Restored visible SVG leader lines with `data-zone-line`, opacity `0.95`, and dashoffset `0` at active stations.
- Moved station title plates to the right flank of the circle with constrained width so they do not overlap the video circle.
- Added `cameraRotateX` compensation to `ProjectZone` so the lifted circle counter-tilts against station camera tilt.
- Added station-specific `circleScale` compensation in `DrawingPackagePage.tsx`:
  - A `{ x: 1.01, y: 1.02 }`
  - B `{ x: 1, y: 1.15 }`
  - C `{ x: 1, y: 1 }`
  - D `{ x: 0.91, y: 0.93 }`
- Restored Station B camera scale to `6.5` so its detail UI matches the other stations.
- Build: ✅ green
- Browser verification: ✅ `975×550`, `1440×900`, zero console/page errors

### Phase 3.8 — Paper desk projection pass & tracking (this session)
- Switched the station detail experience away from the old substrate-space readable circle/label path.
- Added `StationProjectionOverlay` in `DrawingPackagePage.tsx`:
  - fixed screen-space projection layer for active A/B/C/D stations
  - projection origin ring, vertical leader, translucent beam, lifted circular object, and crisp metadata plate
  - station-specific images: `Billet Receiver Set AR15.webp`, `torque-wrench-03.webp`, `pump-package-04.webp`, `rendering-06.webp`
- Added `isAtRest` camera state tracking to trigger the projection UI overlay exactly at the end of camera transit.
- Decoupled GSAP `svgOrigin` rendering logic. Adopted standard CSS `transform-box: fill-box` transitions for substrate origin dots, fixing a severe bounding-box calculation bug (`-1545px`).
- Successfully tracked real-time screen-space coordinates using `requestAnimationFrame` and CSS variables (`--origin-x`, `--origin-y`) so the projection beam firmly tracks the underlying blueprint across viewports.

### Phase 4 — Prune (this session)
- Removed duplicate, unneeded 3D substrate-space detail circles.
- Stripped heavy legacy decorations (`lineRef`, `circleRef`, `labelRef`, `textRef`) and properties (`projectionMode`, `videoSrc`, `cameraRotateX`) from `ProjectZone.tsx`.
- Removed `pathD`, `circleStyle`, and `circleScale` from `ProjectZoneLayout` objects.
- Condensation complete: `ProjectZone.tsx` is now a pure layout anchor.

## WHAT REMAINS

### Phase 5 — Content and Final Polish
- Delete duplicate substrates: `Machined Forging (1).svg`, `(22).svg`, `-11.AI.svg`
- Convert hero word-cycle from `useState`/`setInterval` to CSS keyframes

## PROTECTED ASSETS

- Must keep: `public/assets/images/Lower Receiver_Final.svg`
- Must keep detail images; they are now used by `StationProjectionOverlay`:
  - `torque-wrench-03.webp`, `Billet Receiver Set AR15.webp`
  - `pump-package-04.webp`, `rendering-06.webp`

## SUBSTRATE MIGRATION REFERENCE

| Axis | Old | New | Scale Factor |
|------|-----|-----|-------------|
| X | 8800 | 1625 | 5.415 |
| Y | 6800 | 1075 | 6.326 |
| Camera zoom | 1.2 | 6.5 | 5.415× |

## EXACT NEXT ACTION

Delete dead/duplicate background SVG assets from the repository to reduce bloat, then wire up the hero word-cycle animations and perform the final production QA pass across the `/drawing-package` route.
