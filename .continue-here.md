# Continue Here

Date: 2026-04-14
Task: add new project scenes (Armament, Renderings, Metrology) to ShowreelRemixV2

---

## Current Task

Extending `ShowreelRemixV2` with three new project scenes. All existing scenes (Pump, Torque, PumpTracker) are fully wired with image sequences and text clouds — verified working.

## Exact Stop Point

Session ended after:
- imageSequence crossfade rendering added to RemixProjectScene.tsx
- Image sequences + text clouds wired into all 3 existing V2 project scenes
- ShowreelRemixV2 rendered to MP4 and wired into portfolio page EngineeringReel
- All committed and pushed (`f982ccd` on `main`)

No uncommitted changes. Working tree is clean.

## What Is Done

- [x] imageSequence crossfade rendering in RemixProjectScene.tsx
- [x] Pump + PumpTracker phrase sets in resumeText.ts
- [x] Image sequences for all 3 existing project scenes in Root.tsx V2
- [x] Text clouds for all 3 existing project scenes in Root.tsx V2
- [x] ShowreelRemixV2 rendered to MP4 (25.8 MB h264)
- [x] Video placed at public/assets/video/engineering-review-loop.mp4
- [x] EngineeringReel.tsx REEL_SRC activated
- [x] `npx tsc --noEmit` — CLEAN
- [x] All committed and pushed

## What Remains (in order)

1. **Add Armament scene** (projectScenes[3])
   - Add `armamentPhrases` to `resumeText.ts`
   - Add project entry to `projects[]` in Root.tsx V2 defaultProps
   - Add projectScene entry with imageSequence + resumeTextBlocks
   - Add `<TransitionSeries.Sequence>` block in ShowreelRemixV2Video.tsx
   - Extend `projectDirections` array (add `"down"`)
   - Update `TOTAL_FRAMES`: `(8+10+12+12+12+12+8)*FPS - 6*20`
   - All data is copy-paste ready in `docs/plans/2026-04-14-session-handoff.md`

2. **Add Renderings scene** (projectScenes[4])
   - Verify image assets exist first (rendering-04, -08, -10, renderings-hero)
   - Same wiring pattern as Armament
   - Update `TOTAL_FRAMES`: `(8+10+12+12+12+12+12+8)*FPS - 7*20`
   - Extend `projectDirections` (add `"up"`)

3. **Metrology scene** (optional, projectScenes[5])
   - Data skeleton in `docs/plans/2026-04-13-session-handoff.md`
   - Images TBD

4. **Re-render video** after adding scenes
   - `cd remotion && npx remotion render src/Root.tsx ShowreelRemixV2 out/showreel-remix-v2.mp4 --codec h264`
   - Copy to `public/assets/video/engineering-review-loop.mp4`

## Exact Next Action

Open `docs/plans/2026-04-14-session-handoff.md` → "Armament scene" section.

1. Add `armamentPhrases` to `remotion/src/remix/resumeText.ts`
2. Add armament project data to `projects[]` in Root.tsx V2 defaultProps
3. Add armament `projectScenes[3]` with imageSequence + resumeTextBlocks
4. Add new `<TransitionSeries.Sequence>` in `ShowreelRemixV2Video.tsx`
5. Update `TOTAL_FRAMES` and `projectDirections`
6. Run `npx tsc --noEmit`

## Blockers

None. All data is spec'd in the handoff doc.
