---
last_updated: 2026-05-11T18:30:00Z
status: Station C and D camera stops recalibrated at 1280x720. C verified framed. D verified at end stop. Need multi-viewport validation (975x550, 1440x900, 390x844) and remaining tasks.
---

# Session Context: Spatial Portfolio

> **For AI agents:** Read `the-plan-worth-doing.md` for the overall vision and architecture.
> This file is your operational context: current state, coordinates, and what to do next.
> **Update this file whenever you change a coordinate, add a station, or complete a major task.**
> Mid-session updates are expected and required — don't wait until the end.

---

## Role & Operating Rules

- Act as a **Senior Creative Technologist and GSAP Architect**.
- Do not output generic vertical-scrolling web patterns.
- The viewport is pinned. The substrate moves, not the user.
- `Remotion` is **banned from UI work** — it renders offline MP4/WebM media only.
- All new spatial components must use `IntersectionObserver` for self-contained animation triggers.

---

## Milestone Log *(max 5 entries — drop oldest when adding new)*

1. **GSAP Camera Engine** — Scroll-pinned substrate with whip-pan + scale timeline established.
2. **SVG Substrate** — DXF-derived SVG integrated with `invert(1)` white linework.
3. **3D Perspective Rig** — CSS `perspective: 4000px` + `rotateX: 35°` tilt stop; geometry bug fixed (was 62°/1800px — content went past focal plane).
4. **Station framing corrected** — TRIGGER GUARD RADIUS and BUFFER TUBE SOCKET overlays now sit inside the intended viewport stops instead of clipping off-frame.
5. **Stations C & D camera stops recalibrated** — C (pump) and D (renderings) y-stops corrected from -3900/-3700 to -4980/-4740; transit waypoints updated to match. Both verified visible at 1280x720.

---

## CURRENT STATE & COORDINATES (THE MAP)

> **MUST be updated whenever:** a station is added, a coordinate changes, or a geometry constant changes.

### Map Substrate (The 3D Rig)

- **Asset:** `Lower Receiver-Machined Forging (22).svg`
  - Filter: `invert(1)` → white linework; `mix-blend-screen` on `bg-slate-950`
  - DOF blur state must include `invert(1)` or lines turn black
- **Native Resolution:** 8800px (W) × 6800px (H)
- **CSS 3D Geometry:**
  - Outer container: `perspective: 4000px`, `perspectiveOrigin: 50% 40%`
  - Substrate div: `transformStyle: preserve-3d`
  - Max safe substrate Y at `scale: 1.2`: `4000 / (sin(35°) × 1.2) ≈ 5806px` ← never exceed

### Active Stations & Camera Stops

**Station A** [`id="A"`, TRIGGER GUARD RADIUS]
- Component Position: `left: 1450px, top: 3200px`
- Camera Stop: `x: -1400` on desktop/tablet, `x: -1400 + 0.55*(vw - 975)` under `768px`; `y: -3730 - max(0, 720 - vh)*0.35, scale: 1.2, rotateX: 0`

**Station B** [`id="B"`, BUFFER TUBE SOCKET]
- Component Position: `left: 5567px, top: 833px`
- Camera Stop: `x: -6320 + 0.495*(vw - 975), y: -740 + 0.47*(vh - 550), scale: 1.2, rotateX: 35`

**Station C** [`id="C"`, INDUSTRIAL DEWATERING PUMP]
- Component Position: `left: 3500px, top: 4200px`
- Camera Stop: `x: -3920 + 0.5*(vw - 975), y: -4980 - max(0, 720 - vh)*0.3, scale: 1.2, rotateX: 0`
- *(Recalibrated 2026-05-11: y from -3900 to -4980, x from -3400 to -3920. Verified at 1280x720: top:112, left:417)*

**Station D** [`id="D"`, RENDERINGS]
- Component Position: `left: 6800px, top: 4000px`
- Camera Stop: `x: -7880 + 0.48*(vw - 975), y: -4740 - max(0, 720 - vh)*0.3, scale: 1.2, rotateX: 0`
- *(Recalibrated 2026-05-11: y from -3700 to -4740, x from -6600 to -7880. Verified at 1280x720: top:60, left:426)*

**[Hero / Title Block]** — Pending placement.

### GSAP Timeline Sequence

| Stop | x | y | scale | rotateX |
|---|---|---|---|---|
| Start (Station A) | `-1400` desktop, `-1400 + 0.55*(vw - 975)` under `768px` | `-3730 - max(0, 720 - vh)*0.35` | 1.2 | 0° |
| Whip / transit | -4800 | -2000 | 1.6 | 0° |
| Tilt stop (Station B) | `-6320 + 0.495*(vw - 975)` | `-740 + 0.47*(vh - 550)` | 1.2 | 35° |
| Transit B→C | -5000 | -3800 | 1.4 | 10° |
| Station C | `-3920 + 0.5*(vw - 975)` | `-4980 - max(0, 720 - vh)*0.3` | 1.2 | 0° |
| Transit C→D | -6800 | -3600 | 1.5 | 5° |
| Station D | `-7880 + 0.48*(vw - 975)` | `-4740 - max(0, 720 - vh)*0.3` | 1.2 | 0° |

### Implementation Notes

- `useLayoutEffect` (not `useEffect`) — fires before paint, required for GSAP initial state
- `IntersectionObserver` threshold: `0.2`; each `ProjectZone` animates once per page load and unobserves instead of resetting on exit
- DOF blur is driven by the GSAP scroll timeline, not by Station A's `IntersectionObserver`, so first paint stays at `invert(1) blur(0px)`
- Both DXF-derived substrate candidates are now in `public/assets/images/`: `Lower Receiver-Machined Forging-11.AI.svg` and `Lower Receiver-Machined Forging (22).svg`
- The runtime substrate is now `Lower Receiver-Machined Forging (22).svg`
- Station A keeps the original x baseline and adds a short-viewport y correction: `- max(0, 720 - vh)*0.35`
- Hidden calibration mode exists at `?calibrate=1`; it forces ProjectZone overlays visible and outlines each 600×500 station box
- Station A keeps its desktop baseline stop but adds a narrow-screen x correction below `768px` so the first circle remains visible on phones
- Station B stop is still viewport-aware, and the overlay circle/label now scale down on narrow screens to stay in-frame
- Verified browser matrix (A+B only): `975×550`, `1280×720`, `1440×900`, and `390×844`
- Standalone route check: no navbar, no theme toggle, no home-page sections on `/drawing-package`
- Active docs restored from `docs/plans/archived/` to `docs/plans/`:
  - `docs/plans/2026-05-07-3d-perspective-floor-plane.md`
  - `docs/plans/2026-05-08-3d-rig-session-handoff.md`
- `perspective` on **parent**, `preserve-3d` on **element** — CSS 3D hierarchy requirement
- `labelRef` div uses `perspective: '500px'` (not `transformPerspective` — invalid React CSSProperties)

---

## Next Tasks

1. **Multi-viewport validation for C and D** — verify framing at `975×550`, `1440×900`, and `390×844` using `?calibrate=1` + Playwright. The responsive correction formulas are in place but untested at other viewports.
2. **Integrate Hero / Title Block station** — pending placement. Needs substrate coords and camera stop.
3. **Hook Drawing Package page into main portfolio navigation** — route exists at `/drawing-package` but no nav link yet.
4. **Run `npm run build`** after all calibration changes to verify no regressions.
5. **Consider extending scroll timeline** (`end: '+=4000'`) if 4 stations feels too fast — could go to `+=6000` for more breathing room.

### Calibration Workflow Tips (for next session)

- `window.scrollTo()` does NOT work due to `.drawing-package-route` overflow rules. Use Playwright `PageDown`/`End` keys instead.
- GSAP ScrollTrigger needs real scroll events, not programmatic `scrollTo`.
- The pin spacer height = viewport height + 4000 (the `end: '+=4000'` value).
- Total scrollable distance = 4000px. Station stops at roughly: A=0%, B=~31%, C=~63%, D=~95%.
- To temporarily enable `window.scrollTo` for programmatic scroll: set `document.documentElement.style.overflow = 'auto'` and `document.body.style.overflow = 'auto'`.

### Current Verification Notes

- Focused lint passed for `DrawingPackagePage.tsx` and `ProjectZone.tsx`.
- `npm run build` needs re-run after C/D stop changes.
- Full `npm run lint` still fails on unrelated pre-existing Remotion/UI lint issues.
- Full `npm run test` still fails on unrelated pre-existing image filename case mismatch: expected `.JPG`, received `.jpg`.
