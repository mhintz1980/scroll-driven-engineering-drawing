---
phase: remix-identity-resume-typography
task: 1
total_tasks: 1
status: in_progress
last_updated: 2026-04-12T18:12:00-04:00
---

<current_state>
Active work is a motion-design iteration inside `ShowreelRemix`, specifically the identity scene that used to show a resume image on the right side. The resume image has been replaced with XML-derived animated text, and the current pass now uses overlapping horizontal and vertical text lanes with macro cropped phrases. The user reviewed it in Studio and said it is closer, but still expects more iteration.
</current_state>

<completed_work>
- Removed the right-side resume image from `remotion/src/remix/scenes/RemixIdentityCardScene.tsx`.
- Added `ResumeTextCloud` rendering driven by `resumeTextBlocks`.
- Added `resumeTextBlocks` to `remotion/src/remixSchema.ts`.
- Added XML-derived phrase source and layout helper in `remotion/src/remix/resumeText.ts`.
- Added regression tests in `src/remix-resume-text.test.ts`.
- Fixed a Studio extraction issue by restoring literal inline `defaultProps` in `remotion/src/Root.tsx`.
- Reworked the motion treatment into layered conveyor typography with mixed horizontal and vertical travel.
- Verified with tests, type-check, composition listing, and early/mid/late still renders.
</completed_work>

<remaining_work>
- Tune the identity segment so the motion feels more intentional and less like moving labels.
- Adjust macro scale/cropping if the current giant layers feel too blunt.
- Tune vertical lanes so at least one vertical pass feels strong and deliberate.
- Decide whether the small note panel at bottom right should stay or be removed.
- If the user likes the direction, commit the work.
</remaining_work>

<decisions_made>
- Resume language comes directly from `public/resume-xml.xml`, but distilled into short manufacturing-first phrases instead of long resume sentences.
- `Root.tsx` must keep literal inline `defaultProps` for `ShowreelRemix` and `ShowreelRemix1080` if Studio editing/saving is expected to work.
- Macro type should use lower opacity so it behaves as atmosphere, not as an opaque blocker.
- This segment remains manufacturing-first; software/automation language is allowed only as supporting evidence.
</decisions_made>

<blockers>
- No hard blocker.
- Main open issue is qualitative: the user wants a stronger kinetic-typography feel, and this still needs visual iteration rather than structural debugging.
</blockers>

<context>
The right-side panel is the active design area. Good review frames are around `280`, `360`, and `440` in `ShowreelRemix`. The current visuals are substantially better than the first pass: multiple layers are visible at once, some motion is vertical, and cropped macro text exists. The next pass should focus on better taste, not architecture. Avoid re-breaking Studio by replacing inline `resumeTextBlocks` in `Root.tsx` with imported variables.
</context>

<commands_already_run>
- `npm test -- src/remix-resume-text.test.ts`
- `cd remotion && npm run tsc`
- `cd remotion && npx remotion compositions src/Root.tsx`
- `cd remotion && npx remotion still src/Root.tsx ShowreelRemix /tmp/showreel-remix-identity-a-early.png --frame=280`
- `cd remotion && npx remotion still src/Root.tsx ShowreelRemix /tmp/showreel-remix-identity-a-mid.png --frame=360`
- `cd remotion && npx remotion still src/Root.tsx ShowreelRemix /tmp/showreel-remix-identity-a-late.png --frame=440`
</commands_already_run>

<next_action>
Open `http://127.0.0.1:3333`, scrub `ShowreelRemix` frames `260` to `450`, and make one focused tuning pass in `remotion/src/remix/scenes/RemixIdentityCardScene.tsx` and the inline `resumeTextBlocks` in `remotion/src/Root.tsx` based on the user’s feedback about macro scale, vertical motion, and overlap density.
</next_action>
